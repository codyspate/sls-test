{
	"name": "test-service",
	"version": "1.0.0",
	"description": "",
	"main": "handler.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "babel src --out-dir dist",
		"start": "babel src --out-dir dist && sls offline start --stage dev --skipCacheInvalidation --noAuth --dontPrintOutput"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"dotenv": "^5.0.0",
		"mongodb": "^3.0.2",
		"mongoose": "^5.0.3",
		"node-fetch": "^2.0.0"
	},
	"devDependencies": {
		"babel-cli": "^6.26.0",
		"babel-plugin-transform-object-rest-spread": "^6.26.0",
		"babel-preset-env": "^1.6.1",
		"serverless": "^1.26.0",
		"serverless-offline": "^3.16.0"
	}
}
